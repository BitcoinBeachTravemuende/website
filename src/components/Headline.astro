---
interface Props {
  class?: String;
}

const { class: className = "" } = Astro.props;
---

<h2
  class={`text-center text-4xl md:text-6xl lg:text-8xl text-zinc-500 dark:text-zinc-400 uppercase ext-center  py-8 ${className}`}
>
  <slot />
</h2>
